# Outputs

This directory contains generated AP FRQ outputs.

Current outputs:

- `combined-latex`: combined generated LaTeX/PDF output for the full AP FRQ set.
- `v2-review`: newer review-oriented output set and manifests.

The AP FRQ pipeline now writes into `combined-latex/` by default.
