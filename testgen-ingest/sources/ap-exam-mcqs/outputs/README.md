# Outputs

This directory contains generated AP MCQ outputs.

Current outputs:

- `latex`: generated LaTeX/PDF output directory from the MCQ OCR pipeline.

The MCQ conversion tools now write into `latex/` by default.
