# Current State

## Stable Summary

This workspace contains four separate repos:

- `bnk-decoder`
- `ocr-frq`
- `ocr-mcq`
- `test-generator`

The root folder is a meta-workspace for shared documentation and state.
The root meta-repo is managed through `./workspace-git` and stored under `~/.codex/memories/testgen-suite-root-repo`.

## Current Priorities

- **bnk-decoder** is the active focus. Iterating on decode accuracy guided by ExamView UI screenshots.
- New priority: fingerprint the full `ignore/ExamView/Banks` corpus and separate the decoder into safe bank-family dialects before applying broader fixes.
- Stabilize `ocr-frq`.
- Improve graph and figure handling in the OCR and BNK tooling.
- Define a best-effort import contract for `test-generator`.
- Keep the repo structure flexible so you can work on one app or several at once.

## bnk-decoder Session Notes (2026-05-13)

### Fixes shipped this session

- `pow()` graph evaluator fix — parabola expressions now sample correctly and render as `(A)^N` in Typst/LaTeX
- `x_int1`/`x_int2` no longer leak into graph definitions (`isLikelyGraphExpression` x-boundary fix)
- `inferSlotBasedChoices` — slot-heavy choice templates (vertex/characteristics questions) now correctly reconstruct A/B/C/D choices from post-font refs in column-major binary order
- Benchmark: Pre-Calculus 11 Ch01 stays at 0 bad bodies / 0 bad choices after all changes

### Open — highest priority

1. **Answer key byte location** — 13 confirmed answers (Q1–Q13), exhaustive differential binary analysis across 7 reference points found nothing. DLL analysis of `evengine2.dll` and `evfbb.dll` (in `ignore/ExamView/`) confirmed answer is stored as a single char. `CQuestion::Serialize()` writes it — position still unknown. The 242-byte binary gap is fixed-format but answer is not there. The first 438+ bytes of the choice block binary section are a fixed RTF template (same for all records). See `format-notes.md` for full DLL findings and next steps.

2. **Record over-counting** — 76 clusters vs 30 actual questions in Ch01. Needs record-type byte in binary header.

### Key binary facts discovered this session

- Choice block column-major order confirmed: A, C, B, D (for 2-column layout)
- Choice block structure: `[0x10] A [0x11] content [0x11] C [0x11] content [0x11][0x11] B [0x11] content [0x11] D [0x11] content`
- First "Times New Roman" in each record is a font reference within the choice binary, NOT the display-region end
- Each choice's binary section contains a pair of TNR references 144 bytes apart (for graph-image choices)
- All records have a fixed 242-byte binary gap at the end — non-zero only at positions 2, 226-230, 238
- Narratives are first-class binary objects, stored as `narr1`..`narr6` entries in the v06 payload and selected per question by the 32-bit ordinal immediately before the second `0xfff3c370` tag in each record
- `1..6` map to the catalog entries, while `-1` means no narrative
- Chapter 01 verification: arithmetic/geometric/series preambles are attached from binary; standalone questions remain narrative-free

### Ground truth answers for Ch03 Q1–Q13 (confirmed from ExamView UI)

Q1=A, Q2=C, Q3=B, Q4=C, Q5=B, Q6=B, Q7=B, Q8=A, Q9=B, Q10=B, Q11=B, Q12=C, Q13=D

## Generated State

<!-- BEGIN GENERATED -->
## Updated Repositories

- bnk-decoder: `main` @ `c4a62a8` (0 dirty), last commit: Improve final BNK recovery
- ocr-frq: `main` @ `4cbcdd3` (10 dirty), last commit: Improve password manager recognition for API key field
- ocr-mcq: `main` @ `2ca08c1` (5 dirty), last commit: Improve figure extraction and LaTeX cleanup
- test-generator: `main` @ `3daf2c7` (0 dirty), last commit: Add bank review rendering

This block is regenerated by `scripts/update-workspace-state.mjs`.
<!-- END GENERATED -->
